● # 1. 进目录
  cd "/home/zzx/下载/ROS final course project/src_七上_1779955343_1(1)"

  # 2. 设置环境 + 清缓存 + 发车
  export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
  source /opt/ros/jazzy/setup.bash
  source install/setup.bash
  rm -f /dev/shm/fastrtps_port*
  ros2 launch fishbot_navigation2 bringup_all.launch.py

  如果之前跑过，先杀干净再启动：
  kill -9 $(ps aux | grep -E "rviz2|gz|ros2|component|robot_state|parameter_brid
  ge|cmd_vel_relay|patrol|xacro" | grep -v grep | awk '{print $2}') 2>/dev/null
  
  启动后约 50 秒巡检自动开始，Gazebo + RViz 都会弹窗，照片存 ~/patrol_photos/。