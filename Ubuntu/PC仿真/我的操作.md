
# 🔥 第一步：彻底杀死所有残留进程（最后一次）

bash

运行

```
pkill -f "gz sim|robot_state|parameter_bridge|ros2"
rm -f /dev/shm/fastrtps_*
```
# 🔥 第二步：【正确指令】启动 Gazebo（复制即用）

### 重点：路径加了**双引号**，解决括号语法错误！

bash

运行

```
# 进入项目根目录（带引号，必成功）
cd "/home/zzx/下载/ROS final course project/src_七上_1779955343_1(1)"

# 加载环境
source /opt/ros/jazzy/setup.bash

# 重新编译（必须！修复包找不到问题）
colcon build --symlink-install

# 加载编译后的环境
source install/setup.bash

# 启动仿真
ros2 launch fishbot_description gazebo_sim.launch.py
```

# 🔥 第三步：剩下 3 个终端的【完整正确指令】

## 终端 2 - 启动 Nav2

bash

运行

```
cd "/home/zzx/下载/ROS final course project/src_七上_1779955343_1(1)"
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch fishbot_navigation2 navigation2.launch.py
```

## 终端 3 - 启动控制桥

bash

运行

```
cd "/home/zzx/下载/ROS final course project/src_七上_1779955343_1(1)"
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 run fishbot_navigation2 cmd_vel_relay.py
```

## 终端 4 - 启动自动巡检（先在 RViz 点 2D Pose Estimate）

bash

运行

```
cd "/home/zzx/下载/ROS final course project/src_七上_1779955343_1(1)"
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 run fishbot_navigation2 patrol_node.py --ros-args -p use_sim_time:=True
```

  pkill -9 -f "rviz2|gz
  sim|robot_state|component_container|patrol_node|cmd_vel_relay"

  # 清理共享内存（换过 DDS 才需要）
  rm -f /dev/shm/fastrtps_port*



● 这个更彻底：

  kill -9 $(ps aux | grep -E "rviz2|gz|ros2|component|robot_state|parameter_bridge|cmd_vel_relay|patrol|xacro" | grep -v grep | awk '{print $2}')
  2>/dev/null; sleep 2; rm -f /dev/shm/fastrtps_port*; echo "干净了"

  验证杀干净：
  ps aux | grep -E "rviz|gz|ros2|component" | grep -v grep
  # 无输出 = 全死

  然后一键发车：
  cd "/home/zzx/下载/ROS final course project/src_七上_1779955343_1(1)"
  export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
  source /opt/ros/jazzy/setup.bash
  source install/setup.bash
  rm -f /dev/shm/fastrtps_port*
  ros2 launch fishbot_navigation2 bringup_all.launch.py

✻ Churned for 7s · 1 shell still running

● Background command "Run the all-in-one launch" failed with exit code 1

  Thought for 3s (ctrl+o to expand)

● 好，去试吧。有问题随时说。

![[point_04_20260530_181405 1.jpg]]




